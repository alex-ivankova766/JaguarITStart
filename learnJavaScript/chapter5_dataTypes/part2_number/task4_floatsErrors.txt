Этот цикл – бесконечный. Он никогда не завершится, почему?

let i = 0;
while (i != 10) {
  i += 0.2;
}

Ответ: из-за погрешности в представлении чисел с плавающей точкой;
Не выйдет получить 10 из суммы 0.2 тк 0.2 на самом деле не 0.2 ))))

