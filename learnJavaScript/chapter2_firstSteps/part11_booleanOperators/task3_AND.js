"use strict";

alert(`task3: 1 && null && 2 = ${1 && null && 2}`);
// Выведет alert(null) тк это первое ложное значение
// (запомнить: alert(null) не пустой, а строка "null" тк alert преобразует значения в строки!!!)